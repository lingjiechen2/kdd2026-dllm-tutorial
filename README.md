# KDD 2026 dLLM Tutorial Website

Minimal static website for the tutorial title and tutor information.

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
